# Android release keystore properties (SAMPLE / TEMPLATE)
#
# 複製此檔為 `keystore.properties`（同一資料夾）並填入實際值，
# **絕對不可** commit 到 git（已在 .gitignore 排除）。
#
# 若不填，Gradle 會自動用 debug keystore 簽章 → 產出的 APK 只能開發用，
# 無法作為 release 分發（不同 debug keystore 無法互相升級）。

storeFile=Q:\\path\\to\\Android\\keystore\\uqm-zh-tw.jks
storePassword=CHANGE_ME
keyAlias=uqm-zh-tw
keyPassword=CHANGE_ME
