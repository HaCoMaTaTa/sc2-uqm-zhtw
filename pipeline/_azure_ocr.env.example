# Azure Document Intelligence 憑證（本檔請勿提交到 git）
#
# 使用方式：
#   1. 到 Azure Portal 建立一個「Document Intelligence」資源（F0 免費層即可，
#      每月 500 頁免費，完全涵蓋本手冊 94 頁）。
#   2. 建好後在資源的「Keys and Endpoint」頁面複製 endpoint 與 key。
#   3. 把下面兩行的 <FILL_ME_IN> 換掉，存檔即可。
#   4. 執行 `python ocr_manual_azure.py --pages 11` 先試一頁看看。
#
# 也可以改用環境變數 AZURE_DI_ENDPOINT / AZURE_DI_KEY，那個優先權更高。

AZURE_DI_ENDPOINT=<FILL_ME_IN>
AZURE_DI_KEY=<FILL_ME_IN>
